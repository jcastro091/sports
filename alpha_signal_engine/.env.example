# -------- Data & training configuration --------
# Name (or path) of the dataset CSV used by train_model.py
DATASET_NAME=ConfirmedBets - AllObservations.csv

# Whether to do chronological split instead of random
DO_CHRONO=1          # 1 or 0

# Whether to run PCA
DO_PCA=0             # 1 or 0

# Whether to run hyperparameter tuning
DO_TUNE=0            # 1 or 0


# -------- Integration with sports-repo (for drift / alerts) --------
# Path where sports-repo lives so training can write into it if needed
SPORTS_REPO_ROOT=C:\Users\Jcast\OneDrive\Documents\sports-repo

# Telegram bot token for model drift / training alerts
TELEGRAM_BOT_TOKEN=7928890551:AAGQP6krbyp4_jAedVZTIDXa_QLI2_ynvs4

# Chat ID to send drift / training alerts to (e.g. your private chat or a channel)
PRO_CHAT_ID=-1002756836961

# Optional: different chat for drift-only alerts
DRIFT_ALERT_CHAT_ID=-1002635869925


# -------- Model artifacts (used by ml_predict.py) --------
# Explicit model file path (if not auto-detected)
MODEL_FILE=/app/alpha_signal_engine/data/results/models/strong_winloss_model.pkl

# Alternative env name for the same thing (kept for backward compat)
STRONG_MODEL_PATH=/app/alpha_signal_engine/data/results/models/strong_winloss_model.pkl

# Path to feature names / column list
FEATURE_FILE=/app/alpha_signal_engine/data/results/models/strong_feature_names.json

# Alternative env name for the same thing
STRONG_FEATURE_NAMES=/app/alpha_signal_engine/data/results/models/strong_feature_names.json
