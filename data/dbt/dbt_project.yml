name: 'sharpssignal_data'
version: '1.0.0'
config-version: 2

profile: 'sharpssignal'

model-paths: ['models']
test-paths: ['tests']
seed-paths: ['seeds']
analysis-paths: ['analysis']
macro-paths: ['macros']
snapshot-paths: ['snapshots']

models:
  sharpssignal_data:
    +materialized: view
    staging:
      +schema: "{{ var('schema', target.schema) }}"
    marts:
      +schema: "{{ var('schema', target.schema) }}"
